{
  "buildCommand": "pnpm --filter splitease-next build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "frontend-next/.next",
  "framework": "nextjs",
  "env": {
    "BACKEND_URL": "@backend_url"
  }
}
