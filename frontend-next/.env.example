# ── Backend proxy (server-side only — NOT exposed to browser) ─────────────────
# In local dev this points to the Express server.
# On Vercel / production, set this to your deployed backend URL
# (e.g. https://splitease-api.up.railway.app).
BACKEND_URL=http://localhost:3001
